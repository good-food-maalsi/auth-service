apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
data:
  NODE_ENV: 'production'
  PORT: '8080'
  APP_PORT: '8080'
  # Origines autorisées pour CORS (front Next.js et API). Adapter selon l'URL du front.
  CORS_ORIGINS: 'https://api.good-food.code-with-alex.com,https://app.good-food.code-with-alex.com'
  ADMIN_EMAIL: 'admin@example.com'
  ADMIN_USERNAME: 'admin'
